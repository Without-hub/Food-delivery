<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fooddelivery.mapper.UserMapper">
    <select id="selectByUsername" resultType="com.example.fooddelivery.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, phone, email, create_time, update_time)
        VALUES(#{username}, #{password}, #{phone}, #{email}, NOW(), NOW())
    </insert>

    <select id="selectById" resultType="com.example.fooddelivery.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <update id="updateById">
        UPDATE user
        <set>
            <if test="user.email != null">email = #{user.email},</if>
            <if test="user.phone != null">phone = #{user.phone},</if>
            update_time = NOW()
        </set>
        WHERE id = #{user.id}
    </update>

    <resultMap id="userMap" type="com.example.fooddelivery.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAllUsers" resultMap="userMap">
        SELECT * FROM user
        <where>
            <if test="username != null and username != ''">AND username LIKE CONCAT('%', #{username}, '%')</if>
            <if test="role != null">AND role = #{role}</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countUsers" resultType="int">
        SELECT COUNT(*) FROM user
        <where>
            <if test="username != null and username != ''">AND username LIKE CONCAT('%', #{username}, '%')</if>
            <if test="role != null">AND role = #{role}</if>
        </where>
    </select>

    <update id="updateUserStatus">
        UPDATE user SET status = #{status}, update_time = NOW() WHERE id = #{id}
    </update>

    <select id="countTodayUsers" resultType="int">
        SELECT COUNT(*) FROM user WHERE DATE(create_time) = CURDATE()
    </select>

    <select id="countTotalUsers" resultType="long">
        SELECT COUNT(*) FROM user
    </select>
</mapper>