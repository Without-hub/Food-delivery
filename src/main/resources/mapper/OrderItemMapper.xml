<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fooddelivery.mapper.OrderItemMapper">

    <resultMap id="orderItemMap" type="com.example.fooddelivery.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="dish_id" property="dishId"/>
        <result column="dish_name" property="dishName"/>
        <result column="dish_image" property="dishImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
    </resultMap>

    <insert id="insertBatch">
        INSERT INTO order_item (order_id, dish_id, dish_name, dish_image, price, quantity, subtotal)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.dishId}, #{item.dishName}, #{item.dishImage},
             #{item.price}, #{item.quantity}, #{item.subtotal})
        </foreach>
    </insert>

    <select id="selectByOrderId" resultMap="orderItemMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>
</mapper>
